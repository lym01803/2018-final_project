本目录下有两份源文件：

该部分作业，以pyqt5_cv2_final.py文件作为最终版本，但需要安装有pyqt5库方可运行
如不能正常运行，则以旧版本real_time_detect_new.py为准

real_time_detect_new 在原有界面下进行了一些改动和调整
该版本不会留下可播放的视频文件

pyqt5_cv2_final 版本 支持滑动条缩放图像，图像翻转，笑脸识别，眼部识别等功能
同时，可用鼠标拖动图像
(按住鼠标左键，然后拖动鼠标，即可拖动图像，以便在放大的情况下观看未能显示在屏幕上的部分)。