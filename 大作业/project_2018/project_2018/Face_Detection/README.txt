使用说明：

face_detection.py 会在每次运行时清空the_frame_of_faces_new文件夹，并重新生成
但由于含有cmd命令，故尽量在Windows环境下测试；

如上述版本不能正常运行，请运行face_detection_v2.py版本，该版本不含cmd命令

the_frame_of_faces 是原有文件
the_frame_of_faces_new 是重新生成的

